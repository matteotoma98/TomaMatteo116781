package it.unicam.cs.pa2122.TomaMatteo116781.controller;

class DefaultControllerTest {
    /*
    private DefaultController defaultControllerUnderTest;

    @BeforeEach
    void setUp() {
        defaultControllerUnderTest = new DefaultController();
    }

    @Test
    void testNewPlane() {
        // Setup
        // Run the test

        defaultControllerUnderTest.newPlane(0.0, 0.0);

        // Verify the results
    }

    @Test
    void testGetPlane() {
        // Setup
        // Run the test
        final Plane<Point<Double>> result = defaultControllerUnderTest.getPlane();

        // Verify the results
    }

    @Test
    void testHasPrevious() {
        // Setup
        // Run the test
        final boolean result = defaultControllerUnderTest.hasPrevious();

        // Verify the results
        assertFalse(result);
    }

    @Test
    void testHasNext() {
        // Setup
        // Run the test
        final boolean result = defaultControllerUnderTest.hasNext();

        // Verify the results
        assertFalse(result);
    }

    @Test
    void testPrevious() {
        // Setup
        // Run the test
        final Plane<Point<Double>> result = defaultControllerUnderTest.previous();

        // Verify the results
    }

    @Test
    void testNext() {
        // Setup
        // Run the test
        final Plane<Point<Double>> result = defaultControllerUnderTest.next();

        // Verify the results
    }

    @Test
    void testLoadInstructions() throws Exception {
        // Setup
        // Run the test
        defaultControllerUnderTest.loadInstructions("filePath");

        // Verify the results
    }

    @Test
    void testLoadInstructions_ThrowsIOException() {
        // Setup
        // Run the test
        assertThrows(IOException.class, () -> defaultControllerUnderTest.loadInstructions("filePath"));
    }

    @Test
    void testCreateLOGOFile() throws Exception {
        // Setup
        // Run the test
        final boolean result = defaultControllerUnderTest.createLOGOFile("filepath");

        // Verify the results
        assertFalse(result);
    }

    @Test
    void testCreateLOGOFile_ThrowsIOException() {
        // Setup
        // Run the test
        String filepath="";
        try {
            if (filepath == null) {
                assertThrows(IOException.class, () -> defaultControllerUnderTest.createLOGOFile("filepath"));
            }
        } catch (Exception e) {
            System.out.println(e);
        }
        // Verify the results

    }

    @Test
    void testExecute() {
        // Setup
        // Run the test
        defaultControllerUnderTest.execute("instruction");

        // Verify the results
    }

    @Test
    void testClear() {
        // Setup
        // Run the test
        defaultControllerUnderTest.clear();

        // Verify the results
    }

    @Test
    void testClearNext() {
        // Setup
        // Run the test
        defaultControllerUnderTest.clearNext();

        // Verify the results
    }

     */
}
